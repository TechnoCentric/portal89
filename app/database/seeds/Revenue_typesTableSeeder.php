<?php

class Revenue_typesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('revenue_types')->truncate();

		$revenue_types = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('revenue_types')->insert($revenue_types);
	}

}
