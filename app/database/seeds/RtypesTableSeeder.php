<?php

class RtypesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('rtypes')->truncate();

		$rtypes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('rtypes')->insert($rtypes);
	}

}
